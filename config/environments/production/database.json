{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "mongoose",
      "settings": {
        "host": "${process.env.DATABASE_HOST || 'intelligent-investors-g-shard-00-00.rfzcn.mongodb.net'}",
        "srv": "${process.env.DATABASAE_SRV || true", 
        "port": "${process.env.DATABASE_PORT || 27017}",
        "database": "${process.env.DATABASE_NAME || 'strapi'}",
        "username": "${process.env.DATABASE_USERNAME || 'Dizy'}",
        "password": "${process.env.DATABASE_PASSWORD || 'XUGQ8rkQBdlG3TCI'}"
      },
      "options": {
        "ssl": true
      }
    }
  }
}
